spring:
  application:
    name: CsvKibanaLogsToMockoonMocks
  servlet:
    multipart:
      enabled: true
      max-file-size: 15MB
      max-request-size: 15MB

server:
  port: 8888

minio:
  bucket-name: csv-logs-to-mockoon
  login-key: admin
  password-key: admin759
  minio-url: http://127.0.0.1:9000